// Single import point for the wire contracts shared with the app. The file is
// dependency-free (types and constants only) and gets bundled into the gateway.
export * from '../../../lib/voice/contracts'
